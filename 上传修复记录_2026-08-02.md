# 百度云上传修复记录（2026-08-02）

## 背景
- 2026-08-02 上传任务反复出现 `Slice MD5 mismatch`，每片 500MB 需约 18 分钟重传，5 次重试全部失败后放弃，单批耗时 1.5 小时以上。
- 今早 08:00 批次（`欧尼喵桑_2026-08-02_06-53`）因 bypy 分片失败仍返回退出码 0、脚本误判成功，本地 `_001.ts`（约 523MB）被删除，且百度云上也缺失 —— **确认静默丢数据，该文件无法找回**。
- 上传期间任务持有 flock 锁，后续定时任务全部「跳过任务」，新录制文件在本地堆积。

## 改动内容
文件：`upload_baidu.sh`

### 1. 分片大小 500M → 20M
- `-s 500M` 改为 `-s 20M`。
- 单个分片失败只需重传 20M（秒级），不再每次重传 500M 浪费约 18 分钟。
- 网盘上仍是完整的原文件，分片只影响传输方式。

### 2. 防误删（核心修复）
- 成败判定不再只靠 bypy 退出码：退出码为 0 时还要检查输出中是否含 `Maximum number` / `Error [0-9]` 等失败标记。
- 新增 `verify_remote_sizes()`：清理前逐目录 `bypy list` 核对远端文件大小，与本地完全一致才删除 `./converted`，否则整批保留、判为失败、下次自动重试。
- 防止再次出现「远端缺文件、本地却已删除」的静默丢数据。

### 3. 终止卡住的上传任务
- 已终止 21:30 启动的 bypy 上传进程（PID 1365527/1365529/1365577），并确认 flock 锁已释放。
- `./converted` 中 `_000.mp3`、`_000.ts` 仍在本地，未被误删。
- 定时任务每 10 分钟一次，下次自动按新配置重传。

## 验证
- `bash -n` 语法检查通过。
- `verify_remote_sizes()` 单元测试：
  - 远端缺 `_000.ts`（当前真实状态）→ 返回 1（判失败）✓
  - 远端文件齐全且大小一致 → 返回 0（判成功）✓

## 遗留问题
- 今早 `2026-08-02_06-53/欧尼喵桑_2026-08-02_06-53-58_001.ts` 已丢失，本地与网盘均无副本，无法恢复。
- 上传速度约 0.6MB/s，偏慢，疑似链路不稳定（长期存在 `MD5 mismatch`），建议后续检查网络质量。
