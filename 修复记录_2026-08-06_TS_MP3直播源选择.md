# TS|MP3 直播源选择修复记录（2026-08-06）

## 背景
- 主播配置使用 `TS|MP3` 双格式并开启分段录制时，`000.ts` 可以正常播放，但后续 `001.ts` 至 `012.ts` 被播放器识别为损坏；同编号 MP3 文件正常。
- 录制日志显示，之前的单 TS 录制优先使用抖音 HLS（M3U8）直播源，发生问题的 `TS|MP3` 录制却使用了 FLV 直播源。
- 上传日志中 TS 文件的本地与远端大小一致，确认问题发生在录制/封装阶段，不是上传截断。

## 根因
文件：`main.py`、`src/recording_config.py`

- URL 配置读取阶段能够把 `TS|MP3` 标准化为组合格式。
- FFmpeg 双输出命令构造阶段也使用了 `RecordFormat.parse_multiple()`，可以正确得到 `TS` 和 `MP3`。
- 但更早的直播源选择阶段使用了只支持单格式的 `RecordFormat.parse()`。
- `RecordFormat.parse("TS|MP3", "mp3音频")` 无法在单格式别名表中找到组合值，因此回退为全局默认的 `MP3`，导致 `prefer_hls=False`，抖音平台继而选择 FLV 源。
- FLV 中的 H.264 视频直接复制并切分为 MPEG-TS 时，旧版 FFmpeg 或特定直播码流可能只让首段携带独立解码所需的 SPS/PPS，造成首段正常、后续分段无法独立播放。

## 改动内容

### 1. 组合格式只解析一次
- 在选择直播源之前调用 `RecordFormat.parse_multiple()`。
- 单格式仍返回单元素列表，组合格式按 `|` 拆分、标准化并去重。
- 后续 FFmpeg 命令构造复用同一解析结果，避免不同阶段采用不同解析规则。

### 2. 按全部输出格式决定直播源
- 新增 `formats_prefer_hls()`。
- 只要输出集合包含 `TS` 或 `MP4`，就设置 `prefer_hls=True`。
- 因此即使全局默认格式为 `MP3音频`，URL 单独配置 `TS|MP3` 时也会优先选择 HLS 源。
- 纯 `MP3`、`M4A` 音频录制保持原有直播源选择行为。

### 3. 保留单格式解析
- 固定回退格式 `RecordFormat.parse("TS")` 与 `RecordFormat.parse("M4A")` 继续使用单格式解析。
- 未机械替换全部 `parse()`，避免把原本期望单个 `RecordFormat` 的代码改成列表后产生类型错误。

## 验证
- 新增组合格式解析测试：`ts|MP3|ts` 应规范化并去重为 `TS`、`MP3`。
- 新增直播源偏好回归测试：`TS|MP3 + 全局默认MP3音频` 必须优先 HLS。
- 新增纯音频兼容测试：显式 `MP3` 不应因为全局默认 TS 而优先 HLS。
- 本次新增测试全部通过。
- 完整测试集共运行 41 项：37 项通过、3 项因当前环境没有 FFmpeg 而跳过；另有 1 项既有失败，`test_ffmpeg_command_options` 要求主命令包含 `-reconnect_at_eof`，但修改前的主命令已经不含该参数，与本次修复无关。

## 遗留问题
- 本修复防止后续 `TS|MP3` 录制因错误选择 FLV 源而产生同类分段问题，不能自动修复已经生成的损坏 TS 文件。
- 服务器仍使用 FFmpeg 4.4.2；建议后续升级到包含 H.264 参数集重复写入修复的新版本。
- 上传流程目前主要核对文件大小，尚未使用 `ffprobe` 验证媒体是否可以解封装和解码。
